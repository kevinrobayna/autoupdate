name: autoupdate
on:
  push:
    branches:
      - master
jobs:
  autoupdate:
    name: autoupdate
    runs-on: ubuntu-latest
    steps:
<<<<<<< HEAD
      - uses: docker://kevinrobayna/autoupdate-action:master
=======
      - uses: docker://kevinrobayna/autoupdate-action:v1
>>>>>>> b0467003359565a8d866690b9176e4f0ed08e2a9
        id: autoupdate
        env:
          GITHUB_TOKEN: "${{ secrets.GITHUB_TOKEN }}"
          PR_FILTER: "auto_merge"

      - run: echo 'Merge conflicts found!'
        if: ${{ steps.autoupdate.outputs.conflicted }}

      - run: echo 'No merge conflicts'
        if: ${{ !steps.autoupdate.outputs.conflicted }}