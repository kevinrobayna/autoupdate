name: autoupdate
on:
  push:
#    branches:
#      - master
jobs:
  autoupdate:
    name: autoupdate
    runs-on: ubuntu-latest
    container:
      image: ghcr.io/kevinrobayna/shipit@v1.7.2
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        PR_FILTER: "auto_merge"
